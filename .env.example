# Absolute URL of the deployed site, used for metadata, Open Graph tags and the sitemap.
# Set this in your Vercel project (or local .env.local) once a domain is attached.
NEXT_PUBLIC_SITE_URL=https://ardakutuk.example.com
